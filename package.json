{
	"name": "sophs-space-web",
	"private": true,
	"version": "0.0.0",
	"scripts": {
		"preview": "wrangler pages dev",
		"deploy": "wrangler pages deploy",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@types/node": "^24.10.13",
		"wrangler": "^4.65.0"
	}
}