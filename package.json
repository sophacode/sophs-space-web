{
	"name": "sophs-space-web",
	"private": true,
	"version": "0.0.0",
	"scripts": {
		"build": "echo 'No build needed for static site'",
		"preview": "wrangler pages dev",
		"deploy": "wrangler pages deploy",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"wrangler": "^4.65.0"
	}
}